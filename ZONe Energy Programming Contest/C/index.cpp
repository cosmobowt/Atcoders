#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    int con = 5;
    int con2 = 5;
    vector<int> v;
    vector<int> v;

    while (n--)
    {
        while (con--)
        {
            int value;
            cin >> value;
            v.push_back(value)
        }
    }

    while (con2--)
    {
        v.
    }
}