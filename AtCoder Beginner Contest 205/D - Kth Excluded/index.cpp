#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n, q;
    cin >> n >> q;
    int m = n;
    int p = q;
    int sample = m * 2;

    vector<int> v1, v2, v3;
    while (m--)
    {
        int a;
        v1.push_back(a);
    }
    while (p--)
    {
        int a;
        v2.push_back(a);
    }
    for (int i = 1; i <= sample; i++)
    {
        for (int j = 0; j < n; j++)
        {
        }
    }
}